import React from "react";
import ReactDOM from "react-dom/client";
import ZaffleApp from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ZaffleApp />
  </React.StrictMode>
);
